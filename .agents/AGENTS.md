# Custom Workspace Rules

- **No Auto-Commits**: NEVER execute git commit or git push commands automatically. Always ask for the user's explicit instructions or confirmation before running any git commits or pushes.
